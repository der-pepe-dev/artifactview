<Window x:Class="ArtifactView.App.Views.PluginsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ArtifactView.App.Views"
        Title="Plugins" Width="560" Height="420"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip">
    <DockPanel Margin="12">

        <TextBlock DockPanel.Dock="Top" Text="{Binding Summary}"
                   FontSize="11" Opacity="0.6" Margin="0,0,0,10" />

        <!-- No plugins hint -->
        <TextBlock Text="No plugin manifests discovered.  Place plugin folders in a plugins directory."
                   HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.4"
                   TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasPlugins}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Plugin list -->
        <ScrollViewer>
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasPlugins}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>
            <ItemsControl ItemsSource="{Binding Manifests}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" Padding="10,8" BorderThickness="1"
                                BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}">
                            <StackPanel>
                                <TextBlock FontWeight="SemiBold" FontSize="12">
                                    <Run Text="{Binding Name, Mode=OneWay}" />
                                    <Run Text=" v" Foreground="Gray" />
                                    <Run Text="{Binding Version, Mode=OneWay}" Foreground="Gray" />
                                </TextBlock>
                                <TextBlock FontSize="10" Opacity="0.55" Margin="0,2,0,0">
                                    <Run Text="by " />
                                    <Run Text="{Binding Author, Mode=OneWay}" />
                                    <Run Text="  ·  " />
                                    <Run Text="{Binding License, Mode=OneWay}" />
                                </TextBlock>
                                <TextBlock FontSize="10" Opacity="0.45" Margin="0,2,0,0"
                                           Text="{Binding Capabilities, Converter={x:Static local:CapabilitiesConverter.Instance}}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

    </DockPanel>
</Window>
